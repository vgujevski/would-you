import { users, questions } from '../../api/_DATA'
import { selectAnsweredQuestions, selectNotAnsweredQuestions } from './questionsSlice'

describe('questionsSlice: selectAnsweredQuestions', () => {

  it('should return answered questions', () => {
    
  })

})
